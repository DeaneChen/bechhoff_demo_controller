<UserControl x:Class="PcHostGUI.Views.Rs485SensorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Name}" FontSize="22" FontWeight="SemiBold" />

        <Border Grid.Row="2" CornerRadius="8" BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}" BorderThickness="1" Padding="14">
            <StackPanel>
                <TextBlock Text="RS-485 sensor page (PLC-side protocol pending)" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="{Binding Status}" Margin="0,10,0,0" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" TextWrapping="Wrap" />
                <TextBlock Text="Planned: set Channel/SlaveId, show decoded values, and enable logging." Margin="0,14,0,0" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

