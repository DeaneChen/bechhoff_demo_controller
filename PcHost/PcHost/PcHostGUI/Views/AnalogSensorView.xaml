<UserControl x:Class="PcHostGUI.Views.AnalogSensorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Name}" FontSize="22" FontWeight="SemiBold" />

        <Border Grid.Row="2" CornerRadius="8" BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}" BorderThickness="1" Padding="14">
            <StackPanel>
                <TextBlock Text="PLC symbol (raw INT16)" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBox Width="520" Text="{Binding Symbol, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" />
                    <Button Content="Read once" Command="{Binding ReadOnceCommand}" Margin="10,0,0,0" />
                </StackPanel>

                <TextBlock Text="{Binding Status}" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" Margin="0,12,0,0" />
                <TextBlock Text="{Binding Raw}" FontSize="48" FontWeight="SemiBold" Margin="0,4,0,0" />

                <TextBlock Text="Next: add calibration (mA -> engineering units) and local logging." Margin="0,14,0,0" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
