<UserControl x:Class="PcHostGUI.Views.NimServoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Motor (NiMotion)" FontSize="22" FontWeight="SemiBold" />

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <Border CornerRadius="8" BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}" BorderThickness="1" Padding="14">
                    <StackPanel>
                        <TextBlock Text="Control" FontSize="16" FontWeight="SemiBold" />

                        <WrapPanel Margin="0,12,0,0">
                            <CheckBox Content="Enable" IsChecked="{Binding Enable}" Margin="0,0,16,10" />
                            <CheckBox Content="PowerEnable" IsChecked="{Binding PowerEnable}" Margin="0,0,16,10" />
                            <CheckBox Content="ExtendedPoll" IsChecked="{Binding ExtendedPoll}" Margin="0,0,16,10" />
                            <CheckBox Content="ApplyVmConfig" IsChecked="{Binding ApplyVmConfig}" Margin="0,0,16,10" />
                        </WrapPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Target RPM" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBox Width="120" Text="{Binding TargetVelocity, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <WrapPanel>
                            <Button Content="Apply" Command="{Binding ApplyCommand}" Margin="0,0,10,10" />
                            <Button Content="Start VM" Command="{Binding StartVmCommand}" Margin="0,0,10,10" />
                            <Button Content="Stop" Command="{Binding StopCommand}" Margin="0,0,10,10" />
                            <Button Content="Reset" Command="{Binding ResetCommand}" Margin="0,0,10,10" />
                        </WrapPanel>

                        <TextBlock Text="Tip: Keep Enable=true during experiments; use PowerEnable as Run/Stop." Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" CornerRadius="8" BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}" BorderThickness="1" Padding="14">
                    <StackPanel>
                        <TextBlock Text="Telemetry" FontSize="16" FontWeight="SemiBold" />

                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="CommOk" Margin="0,0,12,6" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CommOk}" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="CommErrorCount" Margin="0,0,12,6" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CommErrorCount}" />

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="LastErrorId" Margin="0,0,12,6" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding LastErrorId}" />

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="CiA402 State" Margin="0,0,12,6" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Cia402State}" />

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="StatusWord" Margin="0,0,12,6" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding StatusWord}" />

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="ControlWord" Margin="0,0,12,6" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding ControlWord}" />

                            <TextBlock Grid.Row="6" Grid.Column="0" Text="VM Eff RPM" Margin="0,0,12,6" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding VmTargetEffRpm}" />

                            <TextBlock Grid.Row="7" Grid.Column="0" Text="VM Actual RPM" Margin="0,0,12,0" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding VmActualRpm}" />

                            <TextBlock Grid.Row="8" Grid.Column="0" Text="DC Bus (V)" Margin="0,0,12,6" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Grid.Row="8" Grid.Column="1" Text="{Binding DcBusVoltage}" />

                            <TextBlock Grid.Row="9" Grid.Column="0" Text="Module Temp (°C)" Margin="0,0,12,0" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Grid.Row="9" Grid.Column="1" Text="{Binding ModuleTemp}" />
                        </Grid>

                        <TextBlock Text="{Binding LastTelemetryUtc, StringFormat=Last update (UTC): {0:HH:mm:ss.fff}}"
                                   Margin="0,12,0,0"
                                   Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
